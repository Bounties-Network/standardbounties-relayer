buildPack: javascript
pipelineConfig:
  pipelines:
    overrides:
      - name: npm-install
        pipeline: release
        stage: build
        agent:
          image: gcr.io/jenkinsxio/builder-nodejs10x
        steps:
          - name: yarn-install
            sh: "yarn install"
        type: replace
