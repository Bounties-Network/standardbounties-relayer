buildPack: javascript
pipelineConfig:
  pipelines:
    overrides:
      - name: npm-install
        pipeline: release
        stage: build
        agent:
          image: "node:10.16.3"
        steps:
          - name: yarn-install
            sh: "yarn install"
        type: replace
